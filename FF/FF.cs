namespace snns;

public static partial class FF
{
	/*
	public class YourClass
	{
		public readonly IList<string> Words { get; private init; }


	The FF class provides a number of generic Free Functions that solve
	problems that come up often, e.g. "Throw an exception on bad input."

		public YourClass(IList<string> words)
		{
			FF.AssertAllUseful(words);

			Words = words;
		}

	Where relevant, FF also introduces extension methods that work the same,
	prefixed with "FF" to avoid pollution the global namespace:

		public YourClass(IEnumerable<string> words)
		{
			words.FFAssertAllUseful();

			Words = words;
		}
	}
	*/
}
